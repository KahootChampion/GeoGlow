import { styled } from "styled-components";

const StyledSecondInformationGroup = styled.div`
  margin-bottom: ${(props) => props.theme.spacing.s};
  grid-area: grid_home_right_middle;
`;

export default StyledSecondInformationGroup;
