import { styled } from "styled-components";

const StyledFirstInformationGroup = styled.div`
  margin-bottom: ${(props) => props.theme.spacing.s};
  grid-area: grid_home_left_middle;
`;

export default StyledFirstInformationGroup;
